name: Build to GHCR
on:
  push:
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1
      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-
      - name: Get version
        id: version
        run: |
          echo ::set-output name=value::"${GITHUB_REF#refs/tags/}"
      - name: Login to ghcr
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_TOKEN }}
      - name: Build piggy-env and push (amd64)
        id: docker_piggy-env-amd64
        uses: docker/build-push-action@v2
        with:
          context: ./piggy-env
          build-args: |
            BUILD_ARCH=amd64
            VERSION=${{ steps.version.outputs.value }}
          file: ./piggy-env/Dockerfile
          builder: ${{ steps.buildx.outputs.name }}
          push: true
          tags: ghcr.io/kongz/piggy-env:${{ steps.version.outputs.value }},ghcr.io/kongz/piggy-env:latest
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache
      - name: piggy-env digest (amd64)
        run: echo ${{ steps.docker_piggy-env-amd64.outputs.digest }}
      - name: Build piggy-env and push (arm64)
        id: docker_piggy-env-arm64
        uses: docker/build-push-action@v2
        with:
          context: ./piggy-env
          build-args: |
            BUILD_ARCH=arm64
            VERSION=${{ steps.version.outputs.value }}
          file: ./piggy-env/Dockerfile
          builder: ${{ steps.buildx.outputs.name }}
          push: true
          tags: ghcr.io/kongz/piggy-env:${{ steps.version.outputs.value }}-arm64
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache
      - name: piggy-env digest (arm64)
        run: echo ${{ steps.docker_piggy-env-arm64.outputs.digest }}
      - name: Build piggy-webhooks and push
        id: docker_piggy-webhooks
        uses: docker/build-push-action@v2
        with:
          context: ./piggy-webhooks
          build-args: |
            VERSION=${{ steps.version.outputs.value }}
          file: ./piggy-webhooks/Dockerfile
          builder: ${{ steps.buildx.outputs.name }}
          push: true
          tags: ghcr.io/kongz/piggy-webhooks:${{ steps.version.outputs.value }},ghcr.io/kongz/piggy-webhooks:latest
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache